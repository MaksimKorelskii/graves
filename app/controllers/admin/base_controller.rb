module Admin
  class BaseController < ApplicationController
    # def authorize(record, query = nil)
    #   super([:admin, record], query)
    # end

    # authorize [:admin, user] # без переопределенного метода authorize использовать так.
  end
end
